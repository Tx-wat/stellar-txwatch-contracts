[package]
name = "test-utils"
version = "0.1.0"
edition = "2021"

[dependencies]
soroban-sdk = { workspace = true, features = ["testutils"] }
alert-registry = { path = "../alert-registry", features = ["testutils"] }
watcher-registry = { path = "../watcher-registry", features = ["testutils"] }
